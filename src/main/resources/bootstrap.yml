#Consfig spring
spring:
  application:
    name: client
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${random.value}
      config:
        enabled: true

#Config  server      
server:
  port: ${my.port:0}
  
#Config feign  
feign:
  hystrix:
    enabled: true        

#Config custom
my:
  prop: descriçao do cliente via yml
