spring:
  application:
    name: client
  cloud:
    consul:
      discovery:
        instanceId: ${spring.application.name}:${random.value}
      config:
        enabled: true
      
server:
  port: ${my.port:8080}
  
my:
  prop: descriçao do cliente via yml
        